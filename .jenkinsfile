pipeline {
    agent any

    stages {
        stage('build') {
            steps {
                echo 'Only git code is commited'
            }
        }
        stage('test') {
            steps {
                echo 'Docker job'
                sh 'sudo docker ps -a'
                sh 'sudo docker run -it -d ubuntu'
            }
        }
        
    }
}
